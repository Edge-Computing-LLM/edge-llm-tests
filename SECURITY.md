# Security policy

Do not open a public issue containing credentials, Secret data, kubeconfig,
private prompts, model responses, internal addresses, or unsanitized cluster
output. Use GitHub's private vulnerability-reporting channel when available.

If sensitive evidence is committed, revoke the affected credential first, then
remove the material from Git history and publish a new sanitized report. Merely
deleting the file in a later commit is not sufficient.
